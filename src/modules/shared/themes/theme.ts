import { buttonTheme } from "./buttons";
import { blueTheme, whiteTheme, mainTheme, blue80, grayTheme, orangeTheme } from "./colors";

export const theme = {
     colors: {
        blueTheme,
        whiteTheme,
        mainTheme,
        blue80,
        grayTheme,
        orangeTheme
     },
     buttons: {
         buttonTheme: buttonTheme
     }
};